{% set titre=titre %}
<div class="card-header">
    <div class="row justify-content-between align-items-center flex-grow-1">
        <div class="col-12 col-md">
            <div class="d-flex justify-content-between align-items-center">
                {% if titre != '' %}
                <h5 class="card-header-title">{{ titre  }}s</h5>
                {% endif %}
                <!-- Datatable Info -->
                <div id="datatableCounterInfo" style="display: none;">
                    <div class="d-flex align-items-center">
                      <span class="font-size-sm mr-3">
                        <span id="datatableCounter">0</span>
                        Selectionné(s)
                      </span>
                       {# <a class="btn btn-sm btn-outline-danger" href="javascript:;">
                            <i class="tio-delete-outlined"></i> Delete
                        </a>#}
                    </div>
                </div>
                <!-- End Datatable Info -->
            </div>
        </div>

        <div class="col-auto">
            <!-- Filter -->
            <div class="row align-items-sm-center">
                <div class="col-sm-auto">
                    {% if critereTitre == "VILLAGES" %}
                 {#   <div class="d-flex align-items-center mr-2">
                        <span class="text-secondary mr-2">{{ critereTitre }}:</span>

                        <!-- Select -->
                        <select class="js-select2-custom js-datatable-filter custom-select-sm" size="1" style="opacity: 0;"
                                data-target-column-index="{{ position }}"
                                data-hs-select2-options='{
                                "minimumResultsForSearch": "",
                                "customClass": "custom-select custom-select-sm custom-select-borderless",
                                "dropdownAutoWidth": true,
                                "width": true
                              }' id="{{ critereTitre }}">
                            <option value="">Tous</option>
                            {% for data in data %}
                                <option value="{{ data.libDepartement }}">{{ data.libDepartement | capitalize }}
                                    <a class=" btn btn-sm btn-outline-warning"  id="exporte" href="{{ path('imprimer',{'mot':data.libDepartement } ) }}" target="__blank" >
                                        <i class="tio-download-to mr-1"></i>
                                    </a>
                                </option>
                            {% endfor %}

                            #}{#<option value="Ouattara">Ouattara</option>
                            <option value="pending">Pending</option>#}{#
                        </select>
                        <!-- End Select -->
                    </div>#}
                    {% endif %}
                </div>
                {% if critereTitre == "VILLAGES" %}
            {#    <div class="col-sm-auto">
                    <div class="d-flex align-items-center mr-2">
                        <span class="text-secondary mr-2">Status:</span>

                        <!-- Select -->
                        <select class="js-select2-custom js-datatable-filter custom-select-sm" size="1" style="opacity: 0;"
                                data-target-column-index="{{ active }}"
                                data-hs-select2-options='{
                                "minimumResultsForSearch": "",
                                "customClass": "custom-select custom-select-sm custom-select-borderless",
                                "dropdownAutoWidth": true,
                                "width": true
                              }'>
                            <option value="">Tous</option>
                            <option value="activé">Activé</option>
                            <option value="désactivé">Désactivé</option>
                        </select>
                        <!-- End Select -->
                    </div>
                </div>#}
                    {{ form_start(form) }}


                    <div class="col-sm-auto mt-4">
                        <div class="d-flex align-items-center mr-2">
                            <span class="text-secondary mr-2">VILLAGES:</span>


                            {{ form_widget(form.village,{'attr':{'class':'form-control border-primary','id':'userinput1','placeholder':'NOM','required':'required'}}) }}
                            <div class="col-sm-auto">
                                <div class="d-flex align-items-center mr-2">
                                    <span class="text-secondary mr-2">TYPES:</span>


                                    {{ form_widget(form.type,{'attr':{'class':'form-control border-primary','id':'userinput1','placeholder':'NOM','required':'required'}}) }}

                                    <div class="col-sm-auto">
                                        <div class="d-flex align-items-center mr-2">
                                            <button type="submit" class="btn btn-primary" style="height: 40px">
                                                <i class="tio-download-to mr-1 mt-1" ></i> Export
                                            </button>

                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div>

                    </div><br>



                    {{ form_end(form) }}
                {#{% endif %}#}

                {% endif %}
                <div class="col-md">
                    <form>
                        <!-- Search -->
                        <div class="input-group input-group-merge input-group-flush">
                            <div class="input-group-prepend">
                                <div class="input-group-text">
                                    <i class="tio-search"></i>
                                </div>
                            </div>
                            <input id="datatableSearch" type="search" class="form-control" placeholder="Recherche..." aria-label="Search users">
                        </div>
                        <!-- End Search -->
                    </form>
                </div>
            </div>
            <!-- End Filter -->
        </div>
    </div>
</div>