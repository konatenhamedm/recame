{% extends 'admin/_includes/index1.html.twig' %}

{% block page_content1 %}
    {# {% form_theme form 'widget/fields-block.html.twig' %}#}
    <div class="modal-header bg-primary">
        <h2 id="editUserModalTitle" class="modal-title " style="margin-top: -15px;color: white">SELECTION DU -{{ titre | upper  }}
            </h2>
        <button type="button" class="close" data-dismiss="modal" style="margin-top: -15px;color: white"
                aria-hidden="true">×
        </button>
    </div>

    <div class="modal-body">
        {# {{ include('includes/ajax/response.html.twig') }} #}
        <div class="row">

           <div class="col-md-12">
               <div class="form-group">
                   <label for="inputGroupMergeGenderSelect" class="input-label">Type de l'acte</label>

                   <div class="input-group input-group-merge">
                       <div class="input-group-prepend">
      <span class="input-group-text">
        <i class="tio-user-outlined"></i>
      </span>
                       </div>
                       <select id="inputGroupMergeGenderSelect" class="custom-select">
                         {#  <option>Choisissez un type d'acte</option>#}
                           <option>VENTE</option>
                           <option>CONSTRUCTION</option>
                           <option>AUTRE</option>
                       </select>
                   </div>
               </div>
           </div>

        </div>


    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-danger text-left" id="myBtn">Annuler</button>
        <button type="button" class="btn btn-primary lancer" >
           Valider
        </button>

        <button class="btn btn-primary" id="lancer"  href="{{ path('acte_new') }}" data-toggle="modal" data-target="#editUserModal" >
            <i class="tio-user-add mr-1"></i>Nouveau
        </button>
    </div>

{% endblock %}
{#{% include 'admin/macros/fichier.html.twig'  %}#}

{% block javascripts %}

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script>
        $("#lancer").hide();
        $(document).ready(function(){
            $("#myBtn").click(function(){
          /*      alert("ffff")*/
                $(".modal-content").modal("hide");

            });
        });
        $(document).ready(function () {
        $('.contenu').hide()
           $('#type').select2();
            $('.click').click(function(){
                $('#myModal').modal({
                    show: true
                })
            });
           $('.lancer').on('click',function () {
             //  alert('jhh')
               $('#lancer').click();
               window.onload();
           });
        });


    </script>
{% endblock %}